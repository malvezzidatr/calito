generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum UserStatus {
  INACTIVE
  ACTIVE
  CANCELLED
}

model User {
  id String @id @default(uuid())
  phone String @unique
  name String
  status UserStatus @default(INACTIVE)
}
