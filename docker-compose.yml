services:
  postgres:
    image: postgres:16-alpine
    container_name: calito-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-calito}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-calito}
      POSTGRES_DB: ${DB_NAME:-calito}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-calito} -d ${DB_NAME:-calito}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
